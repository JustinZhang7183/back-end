server:
  port: 9000

logging:
  level:
    org:
      springframework:
        security:
          oauth2: trace

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8000