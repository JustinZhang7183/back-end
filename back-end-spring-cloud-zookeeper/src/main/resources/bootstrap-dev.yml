spring:
  cloud:
    zookeeper:
      config:
        root: config
        profile-separator: "-"
        enabled: true
      connect-string: ${zookeeper-url}:2181
      discovery:
        root: /services
        instance-host: 192.168.1.100